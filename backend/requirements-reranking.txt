# Optional dependencies for sentence-transformer reranking.
# Install only when ENABLE_RERANKING=true is configured.

--extra-index-url https://download.pytorch.org/whl/cpu
sentence-transformers>=2.2.0
torch==2.12.0+cpu
